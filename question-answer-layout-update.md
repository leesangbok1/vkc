# 질문 상세 레이아웃 & 답변 UI 개선 작업 요약

## 레이아웃 전반
- 질문 상세 페이지를 좌/우 2컬럼 구조로 재구성하여 전체 화면 폭을 활용하도록 확장함.
  - 좌측 컬럼: 질문 카드 + 답변 작성 섹션
  - 우측 컬럼: 답변 목록 + 재답변(댓글) 카드
- 모바일에서는 기존 단일 컬럼 흐름을 유지하되, `답변 작성하기` 토글 버튼으로 폼을 열고 닫도록 구성.
- 좌/우 컬럼 사이에 구분선을 추가해 영역이 명확히 구분되도록 처리.

## 질문 카드 & 메타 정보
- 질문 카드(`FeedCard`)를 메인 피드와 동일한 구조로 렌더링하도록 정리.
- 기존의 메타 스트립(답변 수/작성 일시) 대신 카드 내 `stats` 영역에서 작성 시간을 표시.
- 헤더 영역에는 `← 목록으로`와 `답변 N개`만 노출하여 중복 정보를 제거.

## 답변 카드 & 댓글
- 답변 카드 역시 `FeedCard`로 렌더링하고 신고/공유/ActionBar 등 피드 카드와 동일 패턴을 적용.
- 카드 하단에 `답글 N개` 토글과 `답글 달기` 버튼을 분리 배치하여 UX를 명확하게 개선.
- 답글 토글 클릭 시 재답변 목록이 열리고, `답글 달기`는 언제나 노출되어 입력 폼으로 자동 스크롤 + 포커스 이동.
- 재답변 UI를 카드 기반 스타일로 통일해 메인 피드와 동일한 톤/간격으로 정리.

## 답변 작성 UX
- 데스크톱: 기본 상태에서 “답변 내용을 입력해 보세요…” 버튼만 노출, 클릭 시 전체 RichEditor가 펼쳐지는 형태로 변경.
- 모바일: 기존의 토글 버튼으로 폼을 열고 닫는 동작 유지.

## 반응형
- 최대 폭 제한을 제거하여 넓은 화면에서 좌/우 컬럼이 시원하게 보이도록 함.
- 1023px 이하에서는 자동으로 단일 컬럼으로 전환하고, 구분선/패딩을 제거해 모바일에서도 자연스러운 구성을 유지.

